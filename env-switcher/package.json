{
  "name": "env-switcher",
  "displayName": "Env Switcher",
  "description": "Toggle WordPress environment blocks inside .env files directly from the VS Code status bar.",
  "version": "0.0.1",
  "publisher": "local",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:envSwitcher.select"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "envSwitcher.select",
        "title": "Env Switcher: Select Environment"
      }
    ],
    "configuration": [
      {
        "title": "Env Switcher",
        "properties": {
          "envSwitcher.envSwitcherFile": {
            "type": "string",
            "default": ".env.local",
            "description": "Path to the env file containing WordPress endpoints. Relative paths resolve from the first workspace folder; supports ~/ expansion."
          },
          "envSwitcher.showEnvSwitcher": {
            "type": "boolean",
            "default": true,
            "description": "Show the environment switcher status bar button"
          }
        }
      }
    ]
  }
}
