{
  "name": "git-terminal",
  "displayName": "Git Terminal",
  "description": "Custom command snippets, history, and status bar shortcuts for terminal workflows.",
  "version": "0.0.1",
  "publisher": "local",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:gitTerminal.openCommands",
    "onCommand:gitTerminal.runLastCommand",
    "onCommand:gitTerminal.finishSnippet",
    "onCommand:gitTerminal.history"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "gitTerminal.openCommands",
        "title": "Git Terminal: Open Saved Commands"
      },
      {
        "command": "gitTerminal.runLastCommand",
        "title": "Git Terminal: Run Last Command"
      },
      {
        "command": "gitTerminal.finishSnippet",
        "title": "Git Terminal: Finish Snippet"
      },
      {
        "command": "gitTerminal.history",
        "title": "Git Terminal: History"
      }
    ],
    "configuration": [
      {
        "title": "Git Terminal",
        "properties": {
          "gitTerminal.customTerminals": {
            "type": "array",
            "markdownDescription": "Custom terminal commands to appear as Status Bar buttons. Each item must have a title and command. Example:\n```json\n{\n  \"title\": \"amend\", \"command\": \"git commit --amend --no-verify\"\n}\n```",
            "default": [],
            "items": {
              "type": "object",
              "required": [
                "title",
                "command"
              ],
              "properties": {
                "title": {
                  "type": "string",
                  "description": "Status bar button title and terminal name"
                },
                "command": {
                  "type": "string",
                  "markdownDescription": "Shell snippet. Supports placeholders: ${input:Label}, ${pick:Label|opt1|opt2}, ${env:VAR}, ${workspaceFolder}, ${clipboard}."
                },
                "cwd": {
                  "type": "string",
                  "description": "Working directory for the terminal (optional)"
                },
                "reuse": {
                  "type": "boolean",
                  "description": "Override terminal reuse behavior for this snippet"
                },
                "os": {
                  "type": [
                    "string",
                    "array"
                  ],
                  "description": "Limit snippet to specific operating systems (win32|darwin|linux)"
                }
              }
            }
          },
          "gitTerminal.customTerminalsButtonLabel": {
            "type": "string",
            "default": "git-c",
            "description": "Status Bar button label that opens the saved custom commands Quick Pick"
          },
          "gitTerminal.cursorSymbol": {
            "type": "string",
            "default": "<|>",
            "description": "Marker inside snippets for manual cursor placement before finishing the command"
          },
          "gitTerminal.showPreviewForCustomTerminals": {
            "type": "boolean",
            "default": false,
            "description": "Show a modal preview of the final command before running"
          },
          "gitTerminal.reuseTerminalByTitle": {
            "type": "boolean",
            "default": true,
            "description": "Reuse existing terminal windows matched by snippet title"
          },
          "gitTerminal.customHistorySize": {
            "type": "number",
            "default": 10,
            "minimum": 0,
            "description": "Number of recent custom commands to remember"
          },
          "gitTerminal.confirmDangerousCommands": {
            "type": "boolean",
            "default": true,
            "description": "Ask for confirmation when a command appears dangerous (e.g., git reset --hard)"
          },
          "gitTerminal.pinLastCustomTerminal": {
            "type": "boolean",
            "default": false,
            "description": "Show a separate Status Bar button for the last executed custom command"
          },
          "gitTerminal.pinLastCustomTerminalLabelPrefix": {
            "type": "string",
            "default": "★",
            "description": "Prefix icon/label for the pinned last custom command button"
          },
          "gitTerminal.showStatusBar": {
            "type": "boolean",
            "default": true,
            "description": "Show the saved command status bar button"
          }
        }
      }
    ]
  }
}
