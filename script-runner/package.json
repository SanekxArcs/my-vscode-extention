{
  "name": "script-runner",
  "displayName": "Script Runner",
  "description": "Status bar buttons for package.json scripts with auto-detected package manager support.",
  "version": "0.0.1",
  "publisher": "local",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "workspaceContains:package.json",
    "onStartupFinished",
    "onCommand:scriptRunner.stopRunningScripts"
  ],
  "main": "./extension.js",
  "dependencies": {
    "jsonc-parser": "^3.2.0"
  },
  "contributes": {
    "commands": [
      {
        "command": "scriptRunner.stopRunningScripts",
        "title": "Script Runner: Stop Running Scripts"
      },
      {
        "command": "scriptRunner.showAllScripts",
        "title": "Script Runner: Show All Scripts"
      }
    ],
    "keybindings": [
      {
        "command": "scriptRunner.showAllScripts",
        "key": "ctrl+alt+q"
      }
    ],
    "configuration": [
      {
        "title": "Script Runner",
        "properties": {
          "scriptRunner.useDynamicScriptParsing": {
            "type": "boolean",
            "default": true,
            "description": "Enable dynamic status bar buttons generated from package.json scripts."
          },
          "scriptRunner.workspaceMode": {
            "type": "string",
            "enum": [
              "first",
              "pick",
              "all"
            ],
            "default": "first",
            "description": "How to handle multi-root workspaces when building buttons: first folder, pick per run, or all folders."
          },
          "scriptRunner.excludeScripts": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [
              "test",
              "postinstall",
              "preinstall"
            ],
            "description": "List of scripts to ignore when generating status bar buttons."
          },
          "scriptRunner.maxDynamicScriptButtons": {
            "type": "number",
            "default": 8,
            "minimum": 0,
            "description": "Maximum number of script buttons shown before collapsing into an overflow menu."
          },
          "scriptRunner.scriptOrder": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [
              "dev",
              "start",
              "build",
              "test",
              "lint"
            ],
            "description": "Priority order for scripts. Scripts listed here will appear first."
          },
          "scriptRunner.reuseTerminalForScripts": {
            "type": "boolean",
            "default": true,
            "description": "Reuse dedicated terminals per script instead of creating a new terminal each time."
          },
          "scriptRunner.askBeforeKill": {
            "type": "boolean",
            "default": true,
            "description": "Ask for confirmation before stopping a running script when re-running from the status bar."
          }
        }
      }
    ]
  }
}
